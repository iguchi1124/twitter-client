<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "twitter client" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

    <body>
    <div class="off-canvas-wrap" data-offcanvas>
      <div class="inner-wrap">
        <nav class="tab-bar">
          <section class="left-small">
            <a class="left-off-canvas-toggle menu-icon" href="#"><span></span></a>
          </section>
          <section class="middle tab-bar-section"> 
            <h1 class="title">
              twitter client
              <% if current_user %>
                (@<%= current_user.screen_name %>)
              <% end %>
            </h1> 
          </section> 
        </nav>
        <aside class="left-off-canvas-menu">
          <ul class="off-canvas-list">
            <li><label>MENU</label></li>
            <li>
              <%= link_to 'タイムライン', timeline_path %>
            </li>
            <li>
              <%= link_to '@ツイート', mentions_timeline_path %>
            </li>
            <li>
              <%= link_to 'フォロー', follow_path %>
            </li>
            <li>
              <%= link_to 'フォロワー', followers_path %>
            </li>
            <li>
              <% if current_user %>
                <%= link_to 'ログアウト', sign_out_path %>
              <% else %>
                <%= link_to 'ログイン', '/auth/twitter' %>
              <% end %>
            </li>
          </ul>
        </aside>
        <section class="main-section">
          <% if notice %>
          <div data-alert class="alert-box info">
            <p><%= notice %></p>
            <a href="#" class="close">&times;</a>
          </div>
          <% end %>

          <% if alert %>
          <div data-alert class="alert-box warning">
            <p><%= alert %></p>
            <a href="#" class="close">&times;</a>
          </div>
          <% end %>

          <% if current_user %>
            <%= render 'elements/tweet' %>
          <% end %>

          <%= yield %>
          <%= javascript_include_tag "application" %>
        </section>
        <a class="exit-off-canvas"></a>
      </div>
    </div>
  </body>
</html>